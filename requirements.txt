# Biomarker Timeline — runtime dependencies
# Install with:  pip install -r requirements.txt
#
# WeasyPrint renders the HTML/CSS report to PDF. On most systems the pip
# install is enough; if WeasyPrint complains about missing system libraries,
# see https://doc.courtbouillon.org/weasyprint/stable/first_steps.html
weasyprint>=60
pdfplumber>=0.11
matplotlib>=3.7
fonttools>=4.40
